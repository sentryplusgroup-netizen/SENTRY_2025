# Sentry-project

This repository consists of YOLO AI models we've trained to detect and track deer. You'll also find the full python code that uses the trained model to detect, track, and extract coordinates, and C files used to control the sentry turret.

For the final complete software program, look at sentry_multiprocess
